#!/data/data/com.tool.tree/files/home/bin/bash

if [ "$1" == '-h' ] || [ "$1" == '--help' ] || [ -z "$1" ];then
echo "Use: showtoast 'Xin chào'"
exit
fi

if [ "$1" == "-a" ] || [ "$1" == "--am" ] || [ -z "$shell_progres" ];then
    am broadcast -n com.tool.tree/com.tool.tree.ToastReceiver -a com.tool.tree.SHOW --es text "$2" &>/dev/null
else
    echo "am:[broadcast -n com.tool.tree/com.tool.tree.ToastReceiver -a com.tool.tree.SHOW --es text '$@']"
fi
