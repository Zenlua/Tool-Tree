# Kakathic

listkill() {
echo $1
for pid_text in $(ps -o pid= --ppid $1); do
    listkill $pid_text
done
}

# xử lý
[ -z "$1" ] || echo -e "$1" >&2;
[ -e "$2" ] && rm -fr "$2"

echo
pid_goc="$(pgrep -f $shell_progres)"
kill $(listkill $pid_goc) 2>/dev/null
