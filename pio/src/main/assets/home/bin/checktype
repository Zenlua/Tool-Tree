#!/data/data/com.tool.tree/files/home/bin/bash
# Kakathic

if [ "$1" == "--help" ] || [ "$1" == "-h" ]; then
    echo "Usage: $0 <file|folder>"
    exit 1
fi

[ "$1" ] && gettype.py "$(realpath "$1")" || gettype.py "$(pwd)"
