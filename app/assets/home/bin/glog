# kakathic

if [ "$1" == '-h' ] || [ "$1" == '--help' ] || [ -z "$1" ];then
echo -e "use: gprop key \n If the key does not exist, it can be used. 'gprop key 1' to avoid blank error first time display."
exit
fi

if [ -e $LOG/$1 ];then
cat "$LOG/$1"
else
[ "$2" ] && echo "$2" | tee "$LOG/$1"
fi
