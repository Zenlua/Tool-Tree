<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fitsSystemWindows="true">

    <EditText
        android:id="@+id/kr_param_text"
        android:minHeight="35dp"
        android:background="@android:color/transparent"
        android:textSize="15sp"
        android:padding="0dp"
        android:paddingEnd="32dp"
        android:text=""
        android:maxLines="15"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:breakStrategy="simple" />

    <!-- Chỉ hiện khi nội dung vượt quá 4 dòng, bấm vào để mở toàn màn hình chỉnh sửa -->
    <ImageButton
        android:id="@+id/kr_param_text_expand"
        android:visibility="gone"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="top|end"
        android:background="@android:color/transparent"
        android:padding="4dp"
        android:scaleType="centerInside"
        android:contentDescription="@string/kr_expand_fullscreen"
        android:src="@drawable/ic_expand_fullscreen" />
</FrameLayout>
