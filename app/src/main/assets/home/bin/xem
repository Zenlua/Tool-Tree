#!/data/data/com.tool.tree/files/home/bin/bash

if [ "$1" == '-h' ] || [ "$1" == '--help' ] || [ -z "$1" ]; then
echo "xem http://file.txt"
exit
fi
curl -s --retry 2 --retry-delay 2 -G -L -H "$WEBS" "$@"
