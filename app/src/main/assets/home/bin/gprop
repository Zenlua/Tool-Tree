# Kakathic

if [ "$1" == '-h' ] || [ "$1" == '--help' ] || [ -z "$1" ];then
    echo "Use: gprop ro.vip /path/file" >&2
    exit 1
fi

if [ -f "$2" ]; then
    awk -F= -v k="$1" '$1==k{print $2}' "$2"
fi
