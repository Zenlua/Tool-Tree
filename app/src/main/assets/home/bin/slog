# kakathic

if [ "$1" == '-h' ] || [ "$1" == '--help' ] || [ -z "$1" ];then
    echo -e "Use: slog key 1" >&2
    exit 1
fi

mkdir -p "$LOG"

if [ "$2" ];then
    echo "$2" > "$LOG/$1"
else
    [ "$2" ] || echo -n > "$LOG/$1"
fi
